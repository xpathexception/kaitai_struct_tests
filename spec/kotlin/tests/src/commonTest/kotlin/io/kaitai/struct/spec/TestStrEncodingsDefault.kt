// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec

import io.kaitai.struct.typing.*
import io.kaitai.struct.testformats.StrEncodingsDefault
import kotlin.test.*
import okio.FileSystem
import okio.Path.Companion.toPath
import okio.SYSTEM

class TestStrEncodingsDefault {
    val SRC_DIR = FileSystem.SYSTEM.canonicalize("../../../src/".toPath(true))

    @Test
    fun testStrEncodingsDefault() {
        val path = SRC_DIR.resolve("str_encodings.bin").toString()
        val r: StrEncodingsDefault = StrEncodingsDefault.fromFile(path)

        assertEquals(actual = r.str1(), expected = "Some ASCII")
        assertEquals(actual = r.rest().str2(), expected = "\u3053\u3093\u306b\u3061\u306f")
        assertEquals(actual = r.rest().str3(), expected = "\u3053\u3093\u306b\u3061\u306f")
        assertEquals(actual = r.rest().str4(), expected = "\u2591\u2592\u2593")
    }
}
