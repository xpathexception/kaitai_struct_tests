// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec

import io.kaitai.struct.typing.*
import io.kaitai.struct.testformats.SwitchBytearray
import kotlin.test.*
import okio.FileSystem
import okio.Path.Companion.toPath
import okio.SYSTEM

class TestSwitchBytearray {
    val SRC_DIR = FileSystem.SYSTEM.canonicalize("../../../src/".toPath(true))

    @Test
    fun testSwitchBytearray() {
        val path = SRC_DIR.resolve("switch_opcodes.bin").toString()
        val r: SwitchBytearray = SwitchBytearray.fromFile(path)

        assertEquals(actual = r.opcodes().size, expected = 4)
        assertEquals(actual = r.opcodes()[0].code(), expected = byteArrayOf(83))
        assertEquals(actual = (r.opcodes()[0].body() as SwitchBytearray.Opcode.Strval) /* generic cast */.value(), expected = "foobar")
        assertEquals(actual = r.opcodes()[1].code(), expected = byteArrayOf(73))
        assertEquals(actual = (r.opcodes()[1].body() as SwitchBytearray.Opcode.Intval) /* generic cast */.value(), expected = 66)
        assertEquals(actual = r.opcodes()[2].code(), expected = byteArrayOf(73))
        assertEquals(actual = (r.opcodes()[2].body() as SwitchBytearray.Opcode.Intval) /* generic cast */.value(), expected = 55)
        assertEquals(actual = r.opcodes()[3].code(), expected = byteArrayOf(83))
        assertEquals(actual = (r.opcodes()[3].body() as SwitchBytearray.Opcode.Strval) /* generic cast */.value(), expected = "")
    }
}
